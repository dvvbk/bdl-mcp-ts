{
  "name": "bdl-mcp-server",
  "version": "1.0.0",
  "description": "MCP Server for BDL (Bank Danych Lokalnych) API running on Cloudflare Workers",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "build": "tsc",
    "generate-types": "wrangler types"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241205.0",
    "typescript": "^5.5.0",
    "wrangler": "^3.99.0"
  },
  "engines": {
    "node": ">=18"
  }
}
